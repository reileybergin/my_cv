\usepackage{marvosym} % Allows the use of symbols
\usepackage{parskip}  % Spacing adjustments

% Main font
\usepackage{fontspec}
\setmainfont{Raleway}[
  Path           = ./fonts/,  % Adjust the path to match where you've placed the fonts
  Extension      = .ttf,      % or .otf, depending on the font files you have
  UprightFont    = *-Regular,
  BoldFont       = *-Bold,
  ItalicFont     = *-Italic,
  BoldItalicFont = *-BoldItalic
]
\setsansfont{Raleway}[
  Path           = ./fonts/,
  Extension      = .ttf,
  UprightFont    = *-Regular,
  BoldFont       = *-Bold,
  ItalicFont     = *-Italic,
  BoldItalicFont = *-BoldItalic
]

% Chinese font
\usepackage{xeCJK} % For Chinese text
\setCJKmainfont[Path = ./fonts/]{kaiti.ttf}

% Numbers - different font for digits because Raleway is suboptimal for numbers
% Reference: https://tex.stackexchange.com/a/440174

% Header and footer
\usepackage{fancyhdr}
\usepackage{lastpage} % Required for \pageref{LastPage}
\usepackage{xcolor}   % For text colors
\usepackage{hyperref} % For hyperlinks
\usepackage{url}      % For handling URLs

\pagestyle{fancy}
\fancyhf{}

% Left footer
\fancyfoot[L]{\textcolor{gray}{\footnotesize\emph{Updated \today}}}

% Right footer with URL using \url{}
\fancyfoot[R]{\textcolor{gray}{\footnotesize\emph{Available at \url{https://github.com/reileybergin/my_cv}}}}

% Center footer
\fancyfoot[C]{\textcolor{gray}{\footnotesize\emph{Page \thepage\ of \pageref{LastPage}}}}

% Remove header and footer lines
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% Links configuration
\definecolor{linkcolour}{rgb}{0,0.2,0.6}
\hypersetup{
  colorlinks=true,
  breaklinks=true,
  urlcolor=linkcolour,
  linkcolor=linkcolour,
  citecolor=linkcolour
}
